const calcularExpresion = ({ a, b, c }) => (a * b) + c;                                    
    const resultado1 = {                        
     a:10, b:2, c:5,                           
    };
const resultado = calcularExpresion(resultado1);

console.log(resultado);